
- shell: jq -r '.state | {"{{ item.key }}":.name}' /tmp/resourcegroup_output.json
  register: resourcegroup


- set_stats:
    data:
      ssh_keys: "{{ resourcegroup.stdout }}"
  register: resourcegroup_stats_output

